# OWASP Dependency Check

This directory contains:
- the suppression file used to justify non-exploitable CVEs
- documentation for SCA build-time analysis

Reports are generated automatically in CI and stored as artifacts.