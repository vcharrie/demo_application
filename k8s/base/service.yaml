apiVersion: v1
kind: Service
metadata:
  name: coreservice
  namespace: coreservice
spec:
  type: ClusterIP
  selector:
    app: coreservice
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP